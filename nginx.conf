server {
    listen 80;
    server_name tudominio.com;   # <-- cambia esto

    # Subida de PDFs: permite archivos grandes (100 PDFs pueden pesar bastante)
    client_max_body_size 200M;

    location / {
        proxy_pass         http://app:8000;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;

        # Timeout generoso para procesar muchos PDFs en lote
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }
}

# Descomenta este bloque cuando tengas SSL con certbot:
# server {
#     listen 443 ssl;
#     server_name tudominio.com;
#     client_max_body_size 200M;
#
#     ssl_certificate     /etc/letsencrypt/live/tudominio.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/tudominio.com/privkey.pem;
#
#     location / {
#         proxy_pass         http://app:8000;
#         proxy_set_header   Host $host;
#         proxy_set_header   X-Real-IP $remote_addr;
#         proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_read_timeout 300s;
#         proxy_send_timeout 300s;
#     }
# }
